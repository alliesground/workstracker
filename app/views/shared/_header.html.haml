.row
  .column
    .ui.inverted.menu.attached
      .header.item
        WorksTracker
      .right.menu
        - if user_signed_in?
          .ui.item
            = link_to "My projects", root_path
          .ui.item
            = link_to "Logout", destroy_user_session_path, method: :delete
        - else
          .ui.item
            = link_to "Sign in", new_user_session_path



